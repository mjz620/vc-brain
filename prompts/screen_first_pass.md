# TODO(mingjia): first-pass kill-screen prompt.
# Decide if an opportunity is clearly non-viable before full analysis (spec §2.3).
# Human-owned — the code uses an embedded minimal default until this is filled.
